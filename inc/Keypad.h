//Duplicate inclusion prevention
#ifndef KEYPAD_H_
#define KEYPAD_H_

/*!	Keypad Interface Class.
 *	Provides a means for interfacing with the keypad, and determining what
 *	happens when buttons are pressed.
 *	@todo Write code and documentation
 */
class Keypad {
	private:
	public:
		static bool init();
		static void destroy();
};

#endif /* KEYPAD_H_ */
